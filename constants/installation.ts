export enum LocalFormStep {
  SETUP = 1,
  PROVISIONING = 2,
  READY = 3,
}

export enum FormStep {
  AUTHENTICATION = 1,
  SETUP = 2,
  PROVISIONING = 3,
  READY = 4,
}

export enum CivoMarketpalceFormStep {
  AUTHENTICATION = 0,
  SETUP = 1,
  PROVISIONING = 2,
  READY = 3,
}
