'use client';
import styled from 'styled-components';

export const InputContainer = styled.div`
  display: flex;
  flex-direction: column;
  gap: 8px;

  .MuiFormGroup-root {
    margin-left: 8px;
  }
`;
