import { Meta, StoryObj } from '@storybook/react';

import NoSSRTerminal from './NoSSRTerminal';

const meta: Meta<typeof NoSSRTerminal> = {
  component: NoSSRTerminal,
};

export default meta;

export const Default: StoryObj<typeof NoSSRTerminal> = {};
