'use client';
import React, { FunctionComponent } from 'react';

import Repositories from '@/containers/repositories';

const RepositoriesPage: FunctionComponent = () => {
  return <Repositories />;
};

export default RepositoriesPage;
